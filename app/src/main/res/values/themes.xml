<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CoffeeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

        <!-- Theme color. -->
        <item name="colorPrimary">@color/redPrimary</item>
        <item name="colorPrimaryVariant">@color/redPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/redPrimary</item>
        <item name="colorSecondaryVariant">@color/redPrimary</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar. -->
        <item name="android:statusBarColor">@color/redPrimary</item>

        <!-- Customize your theme here. -->
        <item name="colorTertiary">@color/white</item>
    </style>

    <style name="BottomNavigationView">
        <item name="colorSecondaryContainer">@color/redPrimary</item>
        <item name="colorOnSurface">@color/redPrimary</item>
    </style>

    <style name="Theme.CoffeeApp" parent="Base.Theme.CoffeeApp" />
</resources>